function transform(cargo) {
    return cargo.map(item => item.toUpperCase());
}

// const cargo = ["apples", "ray guns", "oranges"];
// const transformedCargo = transform(cargo);
// console.log(transformedCargo);